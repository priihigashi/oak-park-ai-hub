---
description: Run the focus-partner exit/handoff protocol — write the session handoff doc + update state.
---
Run the focus-partner EXIT / HANDOFF protocol now (Priscila said "/handoff" or "exit"):

1. Update `~/.claude/focus-partner-state.md`: set Last check = today, append a Session Log row (what we did / decided), refresh "This Week" and "Pending Tasks".
2. Create a Google Doc named `HANDOFF_<YYYY-MM-DD>[_short-label]` in Priscila's **Productivity & Routine** folder (id `1b8Cfc8lJhu5unDaxDQIdo4xdN6X7n1nS`), matching HER existing handoff convention:
   - Header: `HANDOFF — <date>` + session/environment context (Claude Code on Windows PC C:\Users\Admin; reads Drive + creates Docs/Calendar via claude.ai MCP as priscila@; CANNOT update Sheets or write via Composio).
   - `✅ DONE THIS SESSION` — numbered, with live URLs / repos / file paths / IDs / shortcuts.
   - `⏳ PENDING` — split into: DEPENDS ON HER (manual, ~5 min each) · NEXT (me) · OLD/open.
   - `IDs / REFERENCES` — key folder/file IDs, repos, and the `/handoff` trigger note.
3. Mirror the updated state file to GitHub (`priihigashi/oak-park-ai-hub`, `.claude/`).
4. Give Priscila a 3-line summary: what got done, what's pending on her, what's next.

Keep it concise and consistent with her existing HANDOFF docs in folder `1b8Cfc8lJhu5unDaxDQIdo4xdN6X7n1nS`.
