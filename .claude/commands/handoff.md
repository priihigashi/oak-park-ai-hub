---
description: Focus Partner handoff command moved to private workspace.
---

# /handoff moved to private repo

The real `/handoff` command now lives in private repo `priihigashi/priscila-workspace` at `.claude/commands/handoff.md`.

Do not store or update Priscila's private Focus Partner state in this public repo.
