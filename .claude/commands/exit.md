---
description: Focus Partner exit command moved to private workspace.
---

# /exit moved to private repo

The real `/exit` command now lives in private repo `priihigashi/priscila-workspace` at `.claude/commands/exit.md`.

Do not store or update Priscila's private Focus Partner state in this public repo.
