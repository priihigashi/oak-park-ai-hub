---
description: Focus Partner command moved to private workspace.
---

# /focus moved to private repo

The real `/focus` command now lives in private repo `priihigashi/priscila-workspace` at `.claude/commands/focus.md`.

Do not store or update Priscila's private Focus Partner state in this public repo.
